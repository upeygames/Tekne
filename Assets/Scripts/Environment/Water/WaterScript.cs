using UnityEngine;

public class WaterScript : MonoBehaviour
{
    void Start()
    {
        // Su yüzeyinin statik bir zemin olarak kullanýlmasý için herhangi bir dinamik hareket yok.
        Debug.Log("WaterScript aktif: Su yüzeyi sabit.");
    }
}
